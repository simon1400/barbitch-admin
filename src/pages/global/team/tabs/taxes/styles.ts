// Локальные классы вкладки «Налоги», нужные больше чем одному её файлу.
// Строки классов перенесены ДОСЛОВНО — вид не менялся.
import { inputCls } from '../../../../../ui/kit'

// Инпут формы во всю ширину (кит + w-full)
export const fieldCls = `${inputCls} w-full`
// Подпись плитки результата / строк сверки (без нижнего отступа)
export const tileLabCls = 'text-[10px] font-bold tracking-[0.06em] uppercase text-ink-soft'
